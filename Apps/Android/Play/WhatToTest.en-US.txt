New and changed

- Clip playback still uses the 720p proxy on the phone. Share and save still use the original camera file.

Fixes

- Opening a long clip for in-app playback no longer crashes the app while that preview is caching.

What to test

- Record a long 4K take, open it from the library, and play it. The proxy should play and the app must stay up. Share or save should still send the original file, not the small preview.
